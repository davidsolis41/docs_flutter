import 'package:flutter/material.dart';

class HomePageLinux extends StatelessWidget {
  const HomePageLinux({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
