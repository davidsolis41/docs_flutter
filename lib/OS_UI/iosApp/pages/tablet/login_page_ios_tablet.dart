import 'package:flutter/cupertino.dart';

class LoginPageIosTablet extends StatelessWidget {
  const LoginPageIosTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
