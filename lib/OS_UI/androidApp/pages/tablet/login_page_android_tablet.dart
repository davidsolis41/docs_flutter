import 'package:flutter/material.dart';

class LoginPageAndroidTablet extends StatelessWidget {
  const LoginPageAndroidTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
