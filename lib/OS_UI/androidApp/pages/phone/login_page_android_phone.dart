import 'package:flutter/material.dart';

class LoginPageAndroidPhone extends StatelessWidget {
  const LoginPageAndroidPhone({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
