import 'package:flutter/cupertino.dart';

class HomePageIosTablet extends StatelessWidget {
  const HomePageIosTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
