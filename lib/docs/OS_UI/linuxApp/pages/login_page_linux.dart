import 'package:flutter/material.dart';

class LoginPageLinux extends StatelessWidget {
  const LoginPageLinux({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
