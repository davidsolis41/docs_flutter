import 'package:flutter/material.dart';

class HomePageAndroidTablet extends StatelessWidget {
  const HomePageAndroidTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
