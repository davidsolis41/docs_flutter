import 'package:fluent_ui/fluent_ui.dart';

class HomePageWindows extends StatelessWidget {
  const HomePageWindows({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
