import 'package:fluent_ui/fluent_ui.dart';

class LoginPageWindows extends StatelessWidget {
  const LoginPageWindows({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
